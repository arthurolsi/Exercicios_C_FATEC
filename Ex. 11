#include <stdio.h>

// Igualdade com function

int igualdade(int a, int b){
    return (a == b);
}

int main()
{
    int numA, numB, valor;
    
    printf("Entre com dois valores \n");
    scanf("%d%d", &numA, &numB);
    valor = igualdade(numA, numB);
    printf(" %d", valor);

    return 0;
}

